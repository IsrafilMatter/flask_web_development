document.addEventListener('DOMContentLoaded', function () {
  // simple focus on first input in forms
  var firstInput = document.querySelector('main form input');
  if (firstInput) firstInput.focus();
});

